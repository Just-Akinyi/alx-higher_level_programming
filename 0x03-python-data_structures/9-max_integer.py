#!/usr/bin/python5
def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None
    sort_list = mylist.sort()
    new_list = sort_list.reverse()
    for i in new_list:
        return new_list
